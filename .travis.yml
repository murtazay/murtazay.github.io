language: node_js
node_js:
  - "node"
cache: npm
script:
  - npm run build
deploy:
  provider: master
  local_dir: dist
  skip_cleanup: true
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
    on:
        branch: master
